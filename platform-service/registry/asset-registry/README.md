# platform-service / registry / asset-registry

## Build

```shell
docker build --tag platform/asset-registry .
```

## Run

```shell
docker run --rm --name platform-asset-registry -p 9000:9000 platform/asset-registry
```

# minio-integrated-service

## minio console

Консоль MiniO доступна по адресе `http://localhost:9501/` с логином `minio` и
паролем `miniosecret` после запуска сервиса с помощью
команды `platformctl service debug`.
